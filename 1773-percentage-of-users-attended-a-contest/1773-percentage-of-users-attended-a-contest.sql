# Write your MySQL query statement below
select r.contest_id, ROUND((COUNT(r.user_id)/(select COUNT(user_name) from Users))*100,2) as percentage from Register r group by r.contest_id order by percentage DESC, contest_id ASC